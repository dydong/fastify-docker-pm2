version: "3"
services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
  app:
    build: .
    container_name: "fastify"
    depends_on:
      - mongodb
    links:
      - mongodb
    ports:
      - 9000:9000
